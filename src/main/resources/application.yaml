spring:
  datasource:
    url: jdbc:h2:mem:veiculosdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  h2:
    console:
      enabled: true

  data:
    redis:
      host: redis
      port: 6379

server:
  port: 8080
